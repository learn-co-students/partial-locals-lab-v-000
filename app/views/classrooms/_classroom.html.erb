Classroom Info
<p>
<%= classroom.course_name %>
<%= classroom.semester %>
</p>

<%# We now want to call out the oldest student in the class: %>
<%= classroom.students.each do |pupil| %>
    <%= render 'students/student', through: {locals: student: pupil} %>
<% end %>