Classroom Info
<%= render partial: 'classroom', locals: { classroom: @classroom } %>

We now want to call out the oldest student in the class:
<% @classroom.students.each do |class_student| %>
    <%= render partial: 'students/student', locals: { classroom_student: class_student } %>
<% end %>
