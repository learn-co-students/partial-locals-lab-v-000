Classroom Info


<% @classroom.students.each do |student| %>
  <%= render partial: 'student', locals: {student: student} %>
<% end %>

<%= render partial: 'classroom', locals: {classroom: @classroom, student: @student} %>
