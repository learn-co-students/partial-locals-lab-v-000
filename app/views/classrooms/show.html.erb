Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to call all the students in the class:
<%= render partial: "students/student", locals: {students: @classroom.students.all} %>
