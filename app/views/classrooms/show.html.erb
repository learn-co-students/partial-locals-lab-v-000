Classroom Info
<%= render partial: 'classroom', locals: { students: @classroom.students } %>

Enrolled Students:
<%= @classroom.students.each do |student| %>
  <%= render partial: 'students/student', locals: { student: student } %> 
<% end %>
