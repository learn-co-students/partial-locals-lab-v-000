<%= render 'classroom' %>




We now want to call out all the students in the class:
<%= @classroom.students.each do |student| %>
<%= render partial: 'students/student', locals:{student: student} %>
<% end %>
