Classroom Info
<%= render partial: 'classrooms/classroom', locals: { classroom: @classroom } %>


<% @classroom.students.each do |student_here| %>
  <%= render partial: 'students/student', locals: { student: student_here } %>
<% end %>
