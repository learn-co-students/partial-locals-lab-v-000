Classroom Info

<%= render partial: 'classroom' %>

Students:
<% @classroom.students.each do |student| %>
  <%= render partial: 'students/student', locals: {classroom_students: @classroom_students, student: student} %>
<% end %>
