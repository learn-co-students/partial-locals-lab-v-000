<%= render partial: "classrooms/classroom", locals: {classroom: @classroom} %>

Let's list all students in the class:
<% @classroom.students.each do |student| %>
	<%= render partial: "students/student", locals: {student: student} %>
<% end %>
