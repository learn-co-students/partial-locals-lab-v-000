<%= render partial: 'classroom', locals: {classroom: @classroom} %>

<% @classroom.students.each do |single_student| %>
  <%= render partial: 'students/student', locals: {student: single_student} %>
<% end %>
