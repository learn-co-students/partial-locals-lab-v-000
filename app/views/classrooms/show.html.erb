Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to show all students in the class:
<% @classroom.students.each do |student_info|
  <%= render partial: "students/student", locals: {student: student_info} %>
<% end %>
