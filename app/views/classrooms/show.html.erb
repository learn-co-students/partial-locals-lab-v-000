Classroom Info
<%= render partial: 'classroom', locals: {classroom: @classroom} %>

We now want to call out the oldest student in the class:

<% @classroom.students.each do |some_student| %>
  <%= render partial: 'students/student', locals: {student: some_student} %>
<% end %>
