Classroom Info

<%= render partial: 'classroom', locals: {classroom: @classroom} %>

All Students

<% @classroom.students.each do |f| %>
  <%= render partial: "students/student", locals: {student: f} %>
<% end %>
