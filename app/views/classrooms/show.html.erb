Classroom Info

<%= render partial: "classroom", locals: {classroom: @classroom} %>

We now want to call out the oldest student in the class:

<% Student.all.each do |current_student| %>
  <%= render partial: "students/student", locals: {student: current_student} %>
<%end%>
