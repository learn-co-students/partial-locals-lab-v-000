<%= render partial: 'form', locals: {student: @student} %>

<!-- renders create student form from the new.html file -->

<!-- directly renders the form in a students/form partial with the form object passed through as a local variable -->
    
<!-- directly renders the form in a students/form partial with the form object passed through as a local variable -->
