<%= form_tag students_path, method: "get" do %>
  <%= label_tag(:name, "Enter Name for Search Here")%><br>
  <%= text_field_tag :name %><br>
  <%= submit_tag("Search") %>
<% end %>

<% @students.each do |student| %>
  <%= render partial: 'student', locals: {student: student} %>
<% end %>