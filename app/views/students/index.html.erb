<%= form_tag students_path, method: :get do %>
  <p>
    <%= text_field_tag :query, params[:query] %>
    <%= submit_tag "Search", name: nil %>
  </p>
<% end %>

<% @students.each do |student_to_display| %>
  <%= render partial: 'student', locals: {student: student_to_display} %>
<% end %>
