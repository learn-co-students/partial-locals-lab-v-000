<!--<%= render partial: "student", locals: {student: Student.search(params[:id])} %>-->
<%= form_tag(students_path) do %>
    <%= text_field_tag :search %>
    <%= submit_tag "Search" %>
<% end %>
<!--<% if @students.blank? %>
  <h4>There are no recipes containing the term <%= params[:search] %>.</h4>
<% end %>-->