<%= form_tag students_path do %>
  <p>
    <%= text_field_tag :searched_student_name %>
    <%= submit_tag "Submit"%>
  </p>
<% end %>

<% @students.each do |student| %>
  <%= render partial: 'student', locals: {student: student} %>
<% end %>
