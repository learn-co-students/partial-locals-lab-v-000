<%= form_tag students_path, method: :get do%>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<% if @students.blank? %>
  <h4>There are no students matching your search.</h4>
<% else %>
  <% @students.each do |student| %>
    <%= render partial: 'student', locals: {student: student} %>
  <% end %>
<% end %>
