<h2>Student Search</h2>

<%= form_tag students_path, method: :get do %>
  <%= label_tag :name %>
  <%= text_field_tag :name %>
  <%= submit_tag 'Search' %>
<% end %>
<br>
Students:
<% @students.each do |s| %>
  <%= render partial: 'student', locals: { student: s } %>
<% end %>