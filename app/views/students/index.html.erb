<h1>Students</h1>
Search by Name:
<%= form_tag students_path, method: "get" do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search Students" %>
  <%= submit_tag "Search" %>
<% end %>

<% if @students.present? %>
  <% @students.each do |this_student| %> 
    <%= render partial: "student", locals: { student: this_student } %>
  <% end %>
<% else %> 
<p>There are no students matching that name %>.</p>
<% end %>
