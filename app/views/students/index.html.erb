<%= form_tag(students_path, method: :get) do %>
  <%= text_field_tag :term, params[:term] %>
  <%= submit_tag 'Search' %>
<% end %>

<% @students.each do |i| %>
  <%= render :partial => "student", locals: {student: i} %>
<% end %>
