<%= form_tag("/search", method: "get") do %>
  <%= label_tag(:name, "Search for:") %>
  <%= text_field_tag(:name) %>
  <%= submit_tag("Search") %>
<% end %>

<% @students.each do |s| %>
  <%= render partial: "student", locals: {student: s} %>
<%end%>
