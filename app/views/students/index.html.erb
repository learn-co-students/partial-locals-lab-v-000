<%= form_tag students_path, method: :get do %>
  <%= text_field_tag :search, params[:search]%>
  <%= submit_tag "Search" %>
<%end%>

<%= @students.each {|student| render partial: 'student', locals: {student: student}}%>
